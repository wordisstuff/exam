"use client";import{useEffect,useState}from"react";import Link from"next/link";import{useRouter}from"next/navigation";import{Shell}from"@/components/Shell";import{questions}from"@/data/questions";import{storage}from"@/lib/storage";import{dashboardStats,performance}from"@/lib/analytics";import{createSession}from"@/lib/engine";import{EXAM_CONFIG}from"@/lib/config";import{fullExamReadiness}from"@/lib/question-validation";import type{Profile,SessionMode}from"@/lib/types";
export default function Dashboard(){const router=useRouter(),[profile,setProfile]=useState<Profile|null>(null),[loaded,setLoaded]=useState(false),[category,setCategory]=useState(questions[0].subcategory),[count,setCount]=useState(5);const attempts=loaded?storage.attempts():[],stats=dashboardStats(attempts),eligible=questions,fullExam=fullExamReadiness(questions),available=eligible.filter(q=>q.subcategory===category).length;useEffect(()=>{const p=storage.profile();if(!p)router.replace("/");else{setProfile(p);setLoaded(true)}},[router]);function start(mode:SessionMode){const pool=mode==="full-exam"?eligible:mode==="quick-test"?eligible:eligible.filter(q=>q.subcategory===category);if(!pool.length)return;const sessionCount=mode==="full-exam"?EXAM_CONFIG.questionCount:mode==="quick-test"?20:Math.min(count,pool.length),s=createSession(mode,pool,sessionCount);storage.saveActive(s);router.push(`/exam/${s.id}`)}if(!profile)return null;const weak=performance(attempts,questions,"subcategory").filter(x=>x.total>=3).slice(0,3);return <Shell><div className="flex items-start justify-between"><div><h1 className="text-3xl font-bold">Welcome back, {profile.name}</h1><p className="muted mt-1">Choose a focused session or review your progress.</p></div><button className="text-sm underline" onClick={()=>{if(confirm("Change user? Your exam history remains on this browser.")){storage.clearProfile();router.push("/")}}}>Change user</button></div><section aria-label="Statistics" className="mt-7 grid grid-cols-2 gap-3 lg:grid-cols-4">{[["Exams Completed",stats.completed],["Average Score",`${stats.average.toFixed(1)}%`],["Best Score",`${stats.best.toFixed(1)}%`],["Questions Answered",stats.answered]].map(x=><div className="card" key={x[0]}><p className="muted text-sm">{x[0]}</p><p className="mt-1 text-2xl font-bold">{x[1]}</p></div>)}</section><section className="mt-8 grid gap-4 lg:grid-cols-3"><article className="card"><h2 className="text-xl font-bold">Full Exam</h2><p className="muted mt-2">{EXAM_CONFIG.questionCount} questions · 5h 30m · exam simulation</p>{fullExam.ready?<button onClick={()=>start("full-exam")} className="btn primary mt-7">Start Full Exam</button>:<><div className="mt-5 rounded-lg bg-amber-50 p-3 text-sm"><strong>Question bank not ready</strong><br/>Full Exam requires {EXAM_CONFIG.questionCount} unique reviewed, source-checked, five-choice questions. Current eligible bank: {fullExam.eligible.length}.</div><button disabled className="btn secondary mt-4 opacity-50">Unavailable</button></>}</article><article className="card"><h2 className="text-xl font-bold">Quick Test</h2><p className="muted mt-2">20 random unique questions. Feedback after completion.</p><button onClick={()=>start("quick-test")} className="btn primary mt-7">Start Quick Test</button></article><article className="card"><h2 className="text-xl font-bold">Practice by Category</h2><label className="mt-3 block text-sm font-bold" htmlFor="cat">Training subcategory</label><select id="cat" value={category} onChange={e=>setCategory(e.target.value)} className="mt-1 w-full rounded border p-2">{[...new Set(eligible.map(q=>q.subcategory))].map(x=><option key={x}>{x}</option>)}</select><label className="mt-3 block text-sm font-bold" htmlFor="count">Questions (available: {available})</label><input id="count" type="number" min="1" max={available} value={Math.min(count,available)} onChange={e=>setCount(Number(e.target.value))} className="mt-1 w-full rounded border p-2"/><button onClick={()=>start("category-practice")} className="btn primary mt-4">Start Practice</button></article></section><section className="card mt-6"><h2 className="font-bold">Weakest meaningful areas</h2>{weak.length?weak.map(x=><p className="mt-2" key={x.name}>{x.name}: {x.percentage.toFixed(0)}% <span className="muted">({x.total} attempts)</span></p>):<p className="muted mt-2">Complete more questions to identify trends. At least 3 attempts per area are needed.</p>}<div className="mt-3 flex gap-4"><Link href="/mistakes" className="underline">Review mistakes</Link><Link href="/weak-areas" className="underline">View analysis</Link></div></section></Shell>}
